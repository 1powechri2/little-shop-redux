<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' type='text/css' href='/dashboard.css' >
    <title>Invoices Dashboard</title>
  </head>
  <body>
    <div class="navbar">
      <h3>Little Shop</h3>
      <div class='first'><a href='/merchants'><h3>Merchants</h3></a></div>
      <div class='second'><a href='/items'><h3>Items</h3></a></div>
      <div class='third'><a href='/invoices'><h3>Invoices</h3></a></div>
    </div>
    <div class='header'>
      <h1>Invoices Dashboard</h1>
    </div>
    <div class='dash-item'>
      <div>
        <h4>Status Percentage</h4>
        <p>Shipped <%= @percent_shipped.round(1) %></p>
        <p>Pending <%= @percent_pending.round(1) %></p>
        <p>Returned <%= @percent_returned.round(1) %></p>
      </div>
      <div>
        <h4>Unit Prices</h4>
        <h5>Highest</h5>
        <% @highest_priced.each do |highest| %>
        <p>Invoice: <a href="/invoices/<%= highest.invoice_id %>"><%= highest.invoice_id %></a></p>
        <% end %>
        <h5>Lowest</h5>
        <% @lowest_priced.each do |lowest| %>
        <p>Invoice: <a href="/invoices/<%= lowest.invoice_id %>"><%= lowest.invoice_id %></a></p>
        <% end %>
      </div>
      <div>
        <h4>Quantity</h4>
        <h5>Highest</h5>
        <p>Invoice: <a href="/invoices/<%= @highest_quantity.id %>"><%= @highest_quantity.id %></a></p>
        <h5>Lowest</h5>
        <p>Invoice: <a href="/invoices/<%= @lowest_quantity.id %>"><%= @lowest_quantity.id %></a></p>
      </div>
    </div>
  </body>
</html>
